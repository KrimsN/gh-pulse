global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # pulse-api подключается в задаче 0.3 (эндпоинт /metrics ещё не существует)
  # - job_name: pulse-api
  #   static_configs:
  #     - targets: ["pulse-api:8000"]
