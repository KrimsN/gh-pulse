[project]
name = "pulse-api"
version = "0.1.0"
description = "Аналитическое REST API GH Pulse поверх ClickHouse/PostgreSQL/Redis"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic-settings",
    "prometheus-client",
    "structlog",
]

[tool.uv]
package = false
